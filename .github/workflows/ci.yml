name: ci

# The jobs, in the order they appear below:
#
#   full             self-hosted: build, unit suites, fuzz regressions, and
#                    the namespace and FRR end-to-end suites. The FRR
#                    scenarios read BPF maps through bpftool, which hosted
#                    runners cannot do.
#   toolchain        every supported clang compiles the BPF object.
#   kernel           unit suites under a chosen kernel in a microVM.
#   cross            the BPF object builds for a non-host architecture.
#   compilers        hosted, stock machine: build and unit suites under
#                    both compilers.
#   coverage         what the host suites reach, reported not gated.
#   static-analysis  scan-build over the engine, any finding fails.
#   style            clang-format, checkpatch and black, as FRR runs them.
#   package          the deb and the rpm, built and installed in a
#                    container per distro.
#
# The nightly fuzz search is separate, in fuzz.yml.
on:
  pull_request:
  # Every branch, so a branch is built before it reaches a pull request. A
  # branch with an open pull request is built twice; the self-hosted jobs skip
  # push events.
  push:
    branches: ['**']
  workflow_dispatch:

# Read-only. Nothing here comments, tags, or publishes.
permissions:
  contents: read

# `full` and `kernel` share one self-hosted VM, so concurrent runs on a ref
# serialise. Cancel the older one rather than queue behind it.
concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  full:
    runs-on: [self-hosted, Linux, X64]
    # Shares one VM with `kernel`; pushes leave it to the pull request.
    if: github.event_name != 'push'
    timeout-minutes: 30
    env:
      BFD_CONTAINER_RUNTIME: podman
      # Suites run under sudo, so keep Python from writing root-owned bytecode
      # the next checkout cannot remove.
      PYTHONDONTWRITEBYTECODE: "1"
    steps:
      - uses: actions/checkout@v4

      # The work directory persists between runs, so a job that died mid-test
      # leaves engines, namespaces, veths and containers behind. A leaked
      # engine still holds an XDP attach and fails the next run's setup.
      - name: clean up anything a previous run left
        run: |
          sudo pkill -f 'bfd_tx 10.77' || true
          sudo pkill -f 'bfd_tx --dplane 50700' || true
          sudo ip netns del bfdrig-a 2>/dev/null || true
          sudo ip netns del bfdrig-b 2>/dev/null || true
          sudo podman rm -f bfdrig-frr-a bfdrig-frr-b 2>/dev/null || true
          sudo ip link del eth-a 2>/dev/null || true
          sudo ip link del rig-a 2>/dev/null || true
          sudo find . -user root -exec rm -rf {} + 2>/dev/null || true
          true

      # clang 21, bpftool, podman and pytest are preinstalled on this VM.
      - name: versions
        run: |
          clang-21 --version | head -1
          bpftool version | head -1
          podman --version
          python3 -m pytest --version
          uname -r

      - name: build
        run: make CLANG=clang-21

      - name: unit suites
        run: sudo make CLANG=clang-21 check

      - name: fuzz regressions
        run: |
          make FUZZ_CC=clang-21 tests/unit/dp_fuzz
          find tests/unit/fuzz-regress -maxdepth 1 -type f ! -name '*.md' \
            -exec ./tests/unit/dp_fuzz {} +

      - name: xdp fuzz regressions
        run: |
          make FUZZ_CC=clang-21 tests/unit/xdp_fuzz
          sudo find tests/unit/fuzz-regress/xdp_fuzz -type f ! -name '*.md' \
            -exec ./tests/unit/xdp_fuzz {} +

      - name: netns end-to-end
        run: sudo make CLANG=clang-21 check-netns

      # Against stock FRR. Needs bpftool map access, so it only runs here.
      - name: frr scenarios
        run: sudo -E make CLANG=clang-21 check-frr

      # Again at the end, so a failed run does not leave the VM dirty.
      - name: leave the machine clean
        if: always()
        run: |
          sudo pkill -f 'bfd_tx 10.77' || true
          sudo pkill -f 'bfd_tx --dplane 50700' || true
          sudo ip netns del bfdrig-a 2>/dev/null || true
          sudo ip netns del bfdrig-b 2>/dev/null || true
          sudo podman rm -f bfdrig-frr-a bfdrig-frr-b 2>/dev/null || true
          sudo ip link del eth-a 2>/dev/null || true
          sudo ip link del rig-a 2>/dev/null || true
          sudo find . -user root -exec rm -rf {} + 2>/dev/null || true
          true

  # Every clang the packaging supports compiles the BPF object; 17 is the floor
  # Build-Depends and BuildRequires ask for. Compile only: the `kernel` job
  # loads a clang-21 object under a real kernel.
  toolchain:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        include:
          - llvm: 17               # the Build-Depends floor
          - llvm: 18
            preinstalled: "true"   # ubuntu-latest ships clang-18
          - llvm: 19
          - llvm: 20
          - llvm: 21
    steps:
      - uses: actions/checkout@v4

      - uses: ./.github/actions/llvm
        with:
          version: ${{ matrix.llvm }}
          preinstalled: ${{ matrix.preinstalled || 'false' }}

      - name: build with clang-${{ matrix.llvm }}
        run: |
          if ! make CLANG=clang-${{ matrix.llvm }} >/tmp/b.log 2>&1; then
            tail -30 /tmp/b.log
            echo "clang-${{ matrix.llvm }} no longer builds the object."
            echo "If a 32-bit atomic came back, widen its target to __u64;"
            echo "see session_state.alive in include/bfd_shared.h."
            exit 1
          fi

  kernel:
    runs-on: [self-hosted, Linux, X64]
    if: github.event_name != 'push'
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        # One kernel for now, the host's series; add rows to cover others.
        kernel:
          - "7.0.0-30-generic"
    steps:
      - uses: actions/checkout@v4

      - name: build on the host
        run: make CLANG=clang-21 && make CLANG=clang-21 tests/unit/bfd_xdp_test.o

      # Kernel images install mode 600; virtme-ng reads one as the runner user.
      - name: make the kernel image readable
        run: |
          test -f /boot/vmlinuz-${{ matrix.kernel }} || {
            sudo apt-get update
            sudo apt-get install -y linux-image-${{ matrix.kernel }}
          }
          sudo chmod 644 /boot/vmlinuz-${{ matrix.kernel }}

      - name: run the suites under ${{ matrix.kernel }}
        run: |
          vng -r ${{ matrix.kernel }} --rwdir=$PWD -- \
            sh -c "cd $PWD && make CLANG=clang-21 check" 2>&1 | tee /tmp/vng.log
          # vng propagates the inner exit status, but the pipe through tee
          # would mask it, so assert on the output too.
          grep -q 'all suites passed' /tmp/vng.log

  # The object is target-independent, but BPFFLAGS takes the UAPI include path
  # from the target triple. Codegen only. A bogus triple must fail on
  # <asm/types.h>, or the aarch64 arm proves nothing.
  cross:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      # bpf_helpers.h is architecture-independent, so the host libbpf-dev
      # serves; only the UAPI headers are per-architecture.
      - uses: ./.github/actions/llvm
        with:
          version: 21

      - name: add arm64 headers
        run: |
          # Ubuntu serves arm64 only from ports.ubuntu.com, so scope the
          # existing sources to amd64 before adding it.
          sudo dpkg --add-architecture arm64
          for f in /etc/apt/sources.list.d/*.sources; do
            sudo sed -i '/^Architectures:/d' "$f"
            sudo sed -i '/^Types:/a Architectures: amd64' "$f"
          done
          sudo cp .github/arm64.sources /etc/apt/sources.list.d/arm64.sources
          sudo apt-get update
          sudo apt-get install -y linux-libc-dev:arm64
          ls -d /usr/include/aarch64-linux-gnu

      - name: build the BPF object for aarch64
        run: make CLANG=clang-21 TRIPLE=aarch64-linux-gnu bfd_xdp.o

      - name: a bogus triple must fail on asm/types.h
        run: |
          touch src/xdp/bfd_xdp.c
          if make CLANG=clang-21 TRIPLE=does-not-exist bfd_xdp.o >/tmp/x.log 2>&1; then
            echo "built with a nonexistent include path; the -I is not"
            echo "load-bearing and the aarch64 arm proves nothing"
            exit 1
          fi
          grep -q 'asm/types.h' /tmp/x.log || {
            echo "failed, but not on the missing header:"; tail -5 /tmp/x.log; exit 1; }

  # Userspace with both compilers; the BPF object is always clang. Checks the
  # multiarch triple comes from the system, not from $(CC).
  compilers:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        cc: [cc, clang-21]
    steps:
      - uses: actions/checkout@v4

      # clang 21 from apt.llvm.org, as the kernel job uses.
      - uses: ./.github/actions/llvm
        with:
          version: 21
          packages: linux-tools-common

      - name: show the multiarch triple
        run: ls /usr/include/$(gcc -print-multiarch) | head -5

      - name: build
        run: make CC=${{ matrix.cc }} CLANG=clang-21

      - name: unit suites
        run: sudo make CC=${{ matrix.cc }} CLANG=clang-21 check

  # A --coverage build of the host suites for fsm.c and dplane.c, written to
  # the job summary. Reported, not gated.
  coverage:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - name: dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y gcovr libbpf-dev
      - name: build and run fsm_run and dp_run instrumented
        run: make CFLAGS="--coverage -O0 -g" test-fsm test-dp
      # Emit a Markdown table for the job summary; the plain report stays in
      # the log.
      - name: report line and branch coverage
        run: |
          gcovr -s --filter 'src/engine/(fsm|dplane)\.c'
          gcovr --json-summary --filter 'src/engine/(fsm|dplane)\.c' \
            | python3 .github/coverage-summary.py >> "$GITHUB_STEP_SUMMARY"

  # Its own job, so a finding is reported as static analysis. --status-bugs
  # fails the job on any finding.
  static-analysis:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - uses: ./.github/actions/llvm
        with:
          version: 21
          packages: clang-tools-21

      - name: scan-build
        run: scan-build-21 --status-bugs -o /tmp/sb make bfd_tx bfd_loader

  style:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - uses: ./.github/actions/llvm
        with:
          version: 21
          packages: clang-format-21

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: install black
        run: pip install black==26.5.1

      - name: tools/checkstyle.sh
        run: tools/checkstyle.sh

  package:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    timeout-minutes: 25
    strategy:
      fail-fast: false
      matrix:
        include:
          - { image: 'debian:12',    suite: debian-12,   kind: deb }
          - { image: 'debian:13',    suite: debian-13,   kind: deb }
          - { image: 'ubuntu:24.04', suite: ubuntu-2404, kind: deb }
          - { image: 'fedora:42',    suite: fedora-42,   kind: rpm }
    container: ${{ matrix.image }}
    steps:
      - name: git for checkout
        run: |
          if command -v apt-get >/dev/null; then
            apt-get update -qq && apt-get install -y -qq git
          else
            dnf install -y -q git
          fi
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: mark workspace safe
        run: git config --global --add safe.directory "$GITHUB_WORKSPACE"
      - name: build ${{ matrix.kind }}
        run: tools/ci-package.sh ${{ matrix.kind }}
      - uses: actions/upload-artifact@v4
        with:
          name: xdp-bfd-${{ matrix.suite }}
          path: dist/**
          if-no-files-found: error
