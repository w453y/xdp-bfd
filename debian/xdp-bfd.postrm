#!/bin/sh
set -e

# On purge only, so a reinstall reuses the uid.
case "$1" in
    purge)
        if getent passwd xdp-bfd >/dev/null; then
            userdel xdp-bfd 2>/dev/null || true
        fi
        ;;
esac

#DEBHELPER#

exit 0
