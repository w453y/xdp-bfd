# tests/e2e; the unit suites are C, run by the Makefile.
[pytest]
# Under sudo a cache would be root-owned.
addopts = -p no:cacheprovider
markers =
    frr: needs a container runtime and pulls the FRR image; not in the
        default netns run
