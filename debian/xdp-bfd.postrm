#!/bin/sh
set -e

# Remove the system user on purge only; remove and upgrade keep it, so a
# reinstall reuses the uid.
case "$1" in
    purge)
        if getent passwd xdp-bfd >/dev/null; then
            userdel xdp-bfd 2>/dev/null || true
        fi
        ;;
esac

#DEBHELPER#

exit 0
